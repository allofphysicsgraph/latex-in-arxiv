\begin{equation}\label{Peterson1}
\Big( (\alpha|\alpha) - 2 \, {\rm ht} \, (\alpha) \Big) c_\alpha =
\sum_{\stackrel{\beta', \beta'' > 0}{\beta' + \beta'' = \alpha}}
(\beta' | \beta'') c_{\beta'} c_{\beta''}
\end{equation}

