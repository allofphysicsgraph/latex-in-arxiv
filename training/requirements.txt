requirements.txt 
tensorflow~=2.15.0  # Or tensorflow-nightly for latest TPU optimizations (see notes below)
transformers~=4.38.0 # Or a recent 4.x version of transformers
dataclasses # Standard library, but good to include for clarity
typing # Standard library, but good to include for clarity

