\begin{equation}
T(\lambda) = \pmatrix{A(\lambda) & B(\lambda) \cr
                                  C(\lambda) & D(\lambda) }; \quad A(\lambda) D(\lambda) - B(\lambda) C(\lambda) =1
\label{TToda}
\end{equation}

