\begin{equation}	\label{12.66new}
\frac{\pd}{\pd \tope{\varphi}_i(x_t)}
=
\frac{\pd}{\pd \ope{\varphi}_i(x_t)}
\qquad
\frac{\pd}{\pd (\pd_\mu\tope{\varphi}_i(x_t))}
=
\frac{\pd}{\pd (\pd_\mu\ope{\varphi}_i(x_t))}
	\end{equation}

